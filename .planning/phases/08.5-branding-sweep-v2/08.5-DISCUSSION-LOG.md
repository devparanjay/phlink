# Phase 8.5 — Discussion Log (`--auto` mode)

> Captured 2026-04-29. Auto-mode = agent-selected recommended defaults, no interactive prompts.
> Per standing user mandate: "make small decisions yourself; report what + why afterwards."

## Mode

`/gsd-discuss-phase 8.5 --auto` — single-pass, all gray areas resolved with recommended defaults. Logged for audit.

## Prior context loaded

- `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`
- `.planning/phases/04.7-branding-sweep-and-scheme-alias/04.7-CONTEXT.md` — prior allowlist methodology + scheme-alias deferral
- `.planning/phases/08-appearance-subsystem/08-CONTEXT.md` — pattern reference + downstream-blocker note
- UAT report (verbal, 2026-04-29 session): radio bugs (fixed in patch `0115`); chrome branding everywhere; URLs still `chrome://`
- repo memory `/memories/repo/phlink-build.md` — macOS dev-build re-sign requirement

## Gray areas identified

1. **Sweep target sourcing** — static GRD grep vs runtime CDP capture vs screenshot OCR
2. **Locale handling** — patch master `.grd` only, or also `.xtb` per-locale
3. **Patch slotting** — single combined patch or split Info.plist / GRD
4. **Regression gate** — extend strings audit, add runtime audit, or both
5. **macOS bundle metadata fields** — `CFBundleName` only, or all three identity fields
6. **Severity classification** — flat list or triaged buckets
7. **Allowlist forensics** — overwrite or preserve history
8. **Harness implementation language** — Python+CDP (existing pattern) vs Node+Playwright
9. **Proper-noun exceptions** — "Chrome Web Store" handling
10. **Scope boundary with 8.6** — touch `chrome://` literal copy now or defer

## `[--auto] Selected all gray areas: [10 areas].`

## Auto-selected decisions

```
[auto] Sweep target sourcing — Q: "How do we know which strings are user-visible?" → Selected: "Runtime CDP capture against launched binary" (recommended; Phase 4.7 falsified the static-grep approach in UAT). → D-01.
[auto] Locale handling — Q: "Patch master .grd only, or also per-locale .xtb?" → Selected: "Master .grd only" (recommended; phlink ships English-only at v1; .xtb is upstream rebase tax). → D-02.
[auto] Patch slotting — Q: "One patch or split?" → Selected: "Split: 0116 Info.plist, 0117 GRD" (recommended; smaller patches, disjoint test gates). → D-03.
[auto] Regression gate — Q: "Static, runtime, or both?" → Selected: "Both — static stays as fast pre-flight, runtime is the new authoritative gate" (recommended; defense-in-depth). → D-04.
[auto] macOS bundle fields — Q: "Which Info.plist identity fields to lock?" → Selected: "CFBundleName + CFBundleDisplayName + CFBundleExecutable" (recommended; Finder/lsregister/screenshot OCR all read these). → D-05.
[auto] Severity classification — Q: "Triage or flat?" → Selected: "Triaged: block / nice-to-have / defer-to-8.7" (recommended; prevents sweep ballooning). → D-06.
[auto] Allowlist forensics — Q: "Overwrite or preserve?" → Selected: "Preserve history with section header + comment trail" (recommended; self-documenting). → D-07.
[auto] Harness language — Q: "Python+CDP or Node+Playwright?" → Selected: "Python + raw CDP websocket" (recommended; matches benchmark_60tab + network-capture stack; no Node toolchain to add). → D-08.
[auto] Proper-noun exceptions — Q: "What about 'Chrome Web Store'?" → Selected: "Allowlist proper nouns referring to Google products phlink reuses" (recommended; renaming creates fraud-vector ambiguity). → D-09.
[auto] Scope boundary with 8.6 — Q: "Touch chrome:// copy now?" → Selected: "Defer to 8.6 — string-only here, scheme + URL copy together later" (recommended; sequencing). → D-10.
```

## Cross-referenced TODOs

None — the unfinished UAT items from this session map directly into 8.5's plan list (08.5-01 through 08.5-06). No open todos in `.planning/STATE.md` that fold into 8.5 with relevance ≥ 0.4.

## Scope-creep redirects

User raised "URLs still `chrome://`" during UAT. **Redirected** to Phase 8.6 (already added to ROADMAP). Phase 8.5 explicitly excludes scheme work (D-10).

## Output

- `.planning/phases/08.5-branding-sweep-v2/08.5-CONTEXT.md` — written.
- `.planning/phases/08.5-branding-sweep-v2/08.5-DISCUSSION-LOG.md` — this file.

## Next step

`/gsd-plan-phase 8.5` (auto-advance via `--auto` chain.md).

---

*Pass count: 1 (auto-mode cap respected).*
*All decisions D-01..D-10 documented in `08.5-CONTEXT.md` §6 with rationales.*
