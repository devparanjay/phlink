---
phase: 08-appearance-subsystem
plan: 02
type: execute
wave: 2
depends_on: ["08-01b"]
files_modified:
  - chrome/browser/ui/color/phlink_palette.cc
  - patches/0111-phlink-phase-08-02-default-light-theme.patch
autonomous: true
requirements:
  - APPR-02
patch_slot: "0111"
must_haves:
  truths:
    - "kPhlinkLight[] in phlink_palette.cc carries the exact 11 hex values from CONTEXT D-08 Light table."
    - "When ColorMode::kLight is active, every kColorPhlink* token resolves to its locked Light hex (verified by phlink_color_mixer_unittests.cc EXPECT_EQ assertions added or extended in this plan)."
    - "Patch 0111 applies cleanly after 0110 and the chrome target builds."
  artifacts:
    - path: chrome/browser/ui/color/phlink_palette.cc
      provides: kPhlinkLight populated with D-08 Light hex
      contains: "0xF8, 0xF4, 0xEC"
    - path: patches/0111-phlink-phase-08-02-default-light-theme.patch
      provides: applied patch
      contains: "kPhlinkLight"
  key_links:
    - from: chrome/browser/ui/color/phlink_palette.cc
      to: .planning/sketches/phase-08/stitch-output.md §2.1
      via: literal hex values mirror the Light theme locked palette
      pattern: "kColorPhlinkAccent.*A8C8E8|0xA8.*0xC8.*0xE8"
---

<objective>
Land the **Light** theme. Per D-08 the eleven Light hex values are LOCKED in `.planning/sketches/phase-08/stitch-output.md §2.1`. This plan does one thing: write those eleven `SkColorSetRGB(...)` literals into `kPhlinkLight[]` in `phlink_palette.cc`, prove they reach the mixer, and ship as patch 0111.

Purpose: Single-purpose, atomic, easily-revertable theme patch. If a future audit wants to retune Light without touching Dark, the diff is exactly this patch.
Output: edited `phlink_palette.cc`, extended unit test asserting Light hex equality, patch 0111.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-appearance-subsystem/08-CONTEXT.md
@.planning/phases/08-appearance-subsystem/08-01b-SUMMARY.md
@.planning/sketches/phase-08/stitch-output.md

<interfaces>
**Locked Light hex values (D-08 / stitch-output.md §2.1):**

| Index | Token | Hex | SkColorSetRGB |
| ----- | ----- | --- | ------------- |
| 0 (kCanvas) | kColorPhlinkCanvas | `#FFFFFF` | `0xFF, 0xFF, 0xFF` |
| 1 (kSurface) | kColorPhlinkSurface | `#F8F4EC` | `0xF8, 0xF4, 0xEC` |
| 2 (kSurfaceAlt) | kColorPhlinkSurfaceAlt | `#F1EDE5` | `0xF1, 0xED, 0xE5` |
| 3 (kCard) | kColorPhlinkCard | `#FFFFFF` | `0xFF, 0xFF, 0xFF` |
| 4 (kText) | kColorPhlinkText | `#1A1A1A` | `0x1A, 0x1A, 0x1A` |
| 5 (kTextMuted) | kColorPhlinkTextMuted | `#525252` | `0x52, 0x52, 0x52` |
| 6 (kHairline) | kColorPhlinkHairline | `#E5E0D6` | `0xE5, 0xE0, 0xD6` |
| 7 (kAccent) | kColorPhlinkAccent | `#A8C8E8` | `0xA8, 0xC8, 0xE8` |
| 8 (kOnAccent) | kColorPhlinkOnAccent | `#1A1A1A` | `0x1A, 0x1A, 0x1A` |
| 9 (kDanger) | kColorPhlinkDanger | `#C0392B` | `0xC0, 0x39, 0x2B` |
| 10 (kSuccess) | kColorPhlinkSuccess | `#2E8540` | `0x2E, 0x85, 0x40` |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate kPhlinkLight[] with D-08 hex values</name>
  <files>chrome/browser/ui/color/phlink_palette.cc</files>
  <behavior>
    - Test 1: `kPhlinkLight[kCanvas] == SkColorSetRGB(0xFF, 0xFF, 0xFF)` … through all 11 indices matching the D-08 table verbatim.
    - Test 2: For ColorMode::kLight, the mixer test (extended in Task 2) asserts `EXPECT_EQ(provider.GetColor(kColorPhlinkAccent), SkColorSetRGB(0xA8, 0xC8, 0xE8))`.
    - Test 3: Order/index alignment with `enum PhlinkToken` from `phlink_palette.h` is preserved.
  </behavior>
  <action>
    Edit `chrome/browser/ui/color/phlink_palette.cc`. Replace the placeholder `kPhlinkLight` initializer block with:
    ```cpp
    const SkColor kPhlinkLight[kPhlinkPaletteSize] = {
        SkColorSetRGB(0xFF, 0xFF, 0xFF),  // [0] kCanvas       — #FFFFFF
        SkColorSetRGB(0xF8, 0xF4, 0xEC),  // [1] kSurface      — #F8F4EC (warm off-white)
        SkColorSetRGB(0xF1, 0xED, 0xE5),  // [2] kSurfaceAlt   — #F1EDE5
        SkColorSetRGB(0xFF, 0xFF, 0xFF),  // [3] kCard         — #FFFFFF
        SkColorSetRGB(0x1A, 0x1A, 0x1A),  // [4] kText         — #1A1A1A
        SkColorSetRGB(0x52, 0x52, 0x52),  // [5] kTextMuted    — #525252
        SkColorSetRGB(0xE5, 0xE0, 0xD6),  // [6] kHairline     — #E5E0D6
        SkColorSetRGB(0xA8, 0xC8, 0xE8),  // [7] kAccent       — #A8C8E8 (Pastel Sky Blue)
        SkColorSetRGB(0x1A, 0x1A, 0x1A),  // [8] kOnAccent     — #1A1A1A (11.4:1 contrast)
        SkColorSetRGB(0xC0, 0x39, 0x2B),  // [9] kDanger       — #C0392B
        SkColorSetRGB(0x2E, 0x85, 0x40),  // [10] kSuccess     — #2E8540
    };
    ```
    Leave `kPhlinkDark` zero-filled (08-03 fills it). Replace any `TODO: filled by patch 0111` comment for the Light array with: `// Locked by Phase 8 D-08; source: .planning/sketches/phase-08/stitch-output.md §2.1.`
  </action>
  <verify>
    <automated>cd /Volumes/Tools/dev/chromium-src/src &amp;&amp; grep -c "SkColorSetRGB" chrome/browser/ui/color/phlink_palette.cc | awk '$1 >= 11 {ok=1} END{exit ok?0:1}'</automated>
  </verify>
  <done>11 SkColorSetRGB literals for Light present and matching the D-08 table; comment cites D-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend phlink_color_mixer_unittests.cc with Light hex assertions</name>
  <files>chrome/browser/ui/color/phlink_color_mixer_unittests.cc</files>
  <behavior>
    - Test 1: `PhlinkColorMixerLightTest.LockedHex_Accent` asserts `provider.GetColor(kColorPhlinkAccent) == SkColorSetRGB(0xA8, 0xC8, 0xE8)`.
    - Test 2: One assertion per locked Light token (11 total) — keeps the test ledger explicit and grep-able.
    - Test 3: Tests live in the same TU added in 08-01b; only new TEST_F bodies are appended.
  </behavior>
  <action>
    Append a `TEST_F(PhlinkColorMixerTest, LockedLightHex)` (or named per existing fixture) that constructs the provider with `key.color_mode = ColorMode::kLight`, calls `AddPhlinkColorMixer`, then runs 11 `EXPECT_EQ` lines mirroring the D-08 Light table. No new headers needed beyond what 08-01b imports.
  </action>
  <verify>
    <automated>cd /Volumes/Tools/dev/chromium-src/src &amp;&amp; export PATH="$HOME/depot_tools:/opt/homebrew/bin:$PATH" &amp;&amp; caffeinate -is autoninja -j 8 -C out/Default unit_tests &amp;&amp; out/Default/unit_tests --gtest_filter='*LockedLightHex*' 2>&amp;1 | tail -3 | grep -q "1 test\|PASSED"</automated>
  </verify>
  <done>LockedLightHex test passes; confirms hex values reach the mixer for ColorMode::kLight.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh patches → produce 0111</name>
  <files>patches/0111-phlink-phase-08-02-default-light-theme.patch</files>
  <action>
    Run `python3 scripts/refresh-patches.py --slot 0111 --subject "phlink: phase 08-02 default Light theme (D-08 locked palette)"` (or fallback to `git format-patch -1 --start-number 111 -o patches/`). Patch must contain only the changes from tasks 1+2. Verify clean apply via `python3 scripts/apply-patches.py --dry-run`.
    Commit body must reference D-08 §Light table and stitch-output.md §2.1.
  </action>
  <verify>
    <automated>cd /Volumes/Tools/dev/phlink &amp;&amp; ls patches/0111-*.patch &amp;&amp; python3 scripts/apply-patches.py --dry-run 2>&amp;1 | tail -3 | grep -qi "ok\|clean\|success"</automated>
  </verify>
  <done>patches/0111-phlink-phase-08-02-default-light-theme.patch exists and dry-run-applies cleanly.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | hex literal accuracy | mitigate | Per-token EXPECT_EQ in unit test. Drift triggers gtest failure. |
| T-08-02-02 | Information Disclosure | telemetry on Light theme rendering | mitigate | No new code paths beyond palette literal definitions. Reviewers reject any UMA. |
</threat_model>

<verification>
- `out/Default/unit_tests --gtest_filter='*LockedLightHex*'` passes.
- `patches/0111-…patch` applies clean after 0110.
- WCAG-AA: deferred to plan 08-05 (will gate both Light and Dark together).
</verification>

<success_criteria>
1. kPhlinkLight[] carries the locked D-08 hex.
2. Mixer unit test proves runtime resolution of every Light token to its locked hex.
3. Patch 0111 lands cleanly on top of 0110.
</success_criteria>

<output>
After completion, create `.planning/phases/08-appearance-subsystem/08-02-SUMMARY.md` with: diff stat, hex table actually written, gtest output, patch slot.
</output>
