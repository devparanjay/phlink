---
phase: 08-appearance-subsystem
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - chrome/browser/ui/color/phlink_palette.cc
  - chrome/browser/ui/color/phlink_color_mixer_unittests.cc
  - patches/0112-phlink-phase-08-03-default-dark-theme.patch
autonomous: true
requirements:
  - APPR-03
patch_slot: "0112"
must_haves:
  truths:
    - "kPhlinkDark[] in phlink_palette.cc carries the exact 11 hex values from CONTEXT D-08 Dark table."
    - "kColorPhlinkOnAccent for Dark resolves to SkColorSetRGB(0x00, 0x00, 0x00) — the critical 'never white' invariant from stitch-output.md §2.2."
    - "When ColorMode::kDark is active, every kColorPhlink* token resolves to its locked Dark hex."
    - "Patch 0112 applies cleanly after 0111 and chrome target builds."
  artifacts:
    - path: chrome/browser/ui/color/phlink_palette.cc
      provides: kPhlinkDark populated with D-08 Dark hex
      contains: "0xF5, 0xE6, 0x8A"
    - path: patches/0112-phlink-phase-08-03-default-dark-theme.patch
      provides: applied patch
      contains: "kPhlinkDark"
  key_links:
    - from: chrome/browser/ui/color/phlink_palette.cc kPhlinkDark[kOnAccent]
      to: stitch-output.md §2.2 critical rule
      via: must equal #000000 (black, NOT white)
      pattern: "kOnAccent.*0x00, 0x00, 0x00"
---

<objective>
Land the **Dark** theme. Per D-08 the eleven Dark hex values are LOCKED in `.planning/sketches/phase-08/stitch-output.md §2.2`, including the critical invariant that `kColorPhlinkOnAccent` is **black (`#000000`)** — white-on-yellow accent fails contrast and is never permitted.

Purpose: Dark is the only theme with a non-obvious foreground rule (black on pastel yellow). Splitting it from Light isolates this risk in one reviewable patch (0112).
Output: edited `phlink_palette.cc` (kPhlinkDark only), extended unit test asserting Dark hex equality, patch 0112.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-appearance-subsystem/08-CONTEXT.md
@.planning/phases/08-appearance-subsystem/08-02-SUMMARY.md
@.planning/sketches/phase-08/stitch-output.md

<interfaces>
**Locked Dark hex values (D-08 / stitch-output.md §2.2):**

| Index | Token | Hex | SkColorSetRGB |
| ----- | ----- | --- | ------------- |
| 0 (kCanvas) | kColorPhlinkCanvas | `#0D0D0D` | `0x0D, 0x0D, 0x0D` |
| 1 (kSurface) | kColorPhlinkSurface | `#181818` | `0x18, 0x18, 0x18` |
| 2 (kSurfaceAlt) | kColorPhlinkSurfaceAlt | `#1F1F1F` | `0x1F, 0x1F, 0x1F` |
| 3 (kCard) | kColorPhlinkCard | `#1F1F1F` | `0x1F, 0x1F, 0x1F` |
| 4 (kText) | kColorPhlinkText | `#FFFFFF` | `0xFF, 0xFF, 0xFF` |
| 5 (kTextMuted) | kColorPhlinkTextMuted | `#A0A0A0` | `0xA0, 0xA0, 0xA0` |
| 6 (kHairline) | kColorPhlinkHairline | `#2A2A2A` | `0x2A, 0x2A, 0x2A` |
| 7 (kAccent) | kColorPhlinkAccent | `#F5E68A` | `0xF5, 0xE6, 0x8A` (Pastel Yellow) |
| 8 (kOnAccent) | kColorPhlinkOnAccent | **`#000000`** | `0x00, 0x00, 0x00` ← **CRITICAL: never white** |
| 9 (kDanger) | kColorPhlinkDanger | `#F5736B` | `0xF5, 0x73, 0x6B` (lightened for dark bg) |
| 10 (kSuccess) | kColorPhlinkSuccess | `#6FE38A` | `0x6F, 0xE3, 0x8A` |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate kPhlinkDark[] with D-08 hex values</name>
  <files>chrome/browser/ui/color/phlink_palette.cc</files>
  <behavior>
    - Test 1: `kPhlinkDark[kOnAccent] == SkColorSetRGB(0x00, 0x00, 0x00)` — the never-white rule.
    - Test 2: `kPhlinkDark[kAccent] == SkColorSetRGB(0xF5, 0xE6, 0x8A)`.
    - Test 3: All 11 indices match D-08 §Dark verbatim.
  </behavior>
  <action>
    Edit `chrome/browser/ui/color/phlink_palette.cc`. Replace the placeholder `kPhlinkDark` initializer with:
    ```cpp
    const SkColor kPhlinkDark[kPhlinkPaletteSize] = {
        SkColorSetRGB(0x0D, 0x0D, 0x0D),  // [0] kCanvas       — #0D0D0D (near-black, slight warmth)
        SkColorSetRGB(0x18, 0x18, 0x18),  // [1] kSurface      — #181818
        SkColorSetRGB(0x1F, 0x1F, 0x1F),  // [2] kSurfaceAlt   — #1F1F1F
        SkColorSetRGB(0x1F, 0x1F, 0x1F),  // [3] kCard         — #1F1F1F
        SkColorSetRGB(0xFF, 0xFF, 0xFF),  // [4] kText         — #FFFFFF
        SkColorSetRGB(0xA0, 0xA0, 0xA0),  // [5] kTextMuted    — #A0A0A0
        SkColorSetRGB(0x2A, 0x2A, 0x2A),  // [6] kHairline     — #2A2A2A
        SkColorSetRGB(0xF5, 0xE6, 0x8A),  // [7] kAccent       — #F5E68A (Pastel Yellow)
        SkColorSetRGB(0x00, 0x00, 0x00),  // [8] kOnAccent     — #000000 ← CRITICAL: never white. 16:1 contrast.
        SkColorSetRGB(0xF5, 0x73, 0x6B),  // [9] kDanger       — #F5736B (lightened for dark)
        SkColorSetRGB(0x6F, 0xE3, 0x8A),  // [10] kSuccess     — #6FE38A
    };
    ```
    Replace the placeholder TODO comment with: `// Locked by Phase 8 D-08; source: .planning/sketches/phase-08/stitch-output.md §2.2. The kOnAccent black is the WCAG 16:1 invariant — DO NOT change to white without re-running stitch validation and updating D-08.`
  </action>
  <verify>
    <automated>cd /Volumes/Tools/dev/chromium-src/src &amp;&amp; grep -A1 "kPhlinkDark\[kPhlinkPaletteSize\]" chrome/browser/ui/color/phlink_palette.cc | grep -q "0x0D, 0x0D, 0x0D" &amp;&amp; grep -B1 -A1 "kOnAccent" chrome/browser/ui/color/phlink_palette.cc | grep -q "0x00, 0x00, 0x00"</automated>
  </verify>
  <done>11 Dark SkColorSetRGB literals present; OnAccent is black; comment cites D-08 and the never-white rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend phlink_color_mixer_unittests.cc with Dark hex assertions including never-white invariant</name>
  <files>chrome/browser/ui/color/phlink_color_mixer_unittests.cc</files>
  <behavior>
    - Test 1: `PhlinkColorMixerTest.LockedDarkHex` runs 11 EXPECT_EQ lines for the Dark table.
    - Test 2: `PhlinkColorMixerTest.OnAccent_DarkIsBlack_NeverWhite` explicitly asserts `provider.GetColor(kColorPhlinkOnAccent) == SK_ColorBLACK` and `!= SK_ColorWHITE` for Dark mode.
  </behavior>
  <action>
    Append two TEST_F bodies analogous to the LockedLightHex test from 08-02. The dedicated `OnAccent_DarkIsBlack_NeverWhite` test is the named gate for the stitch-output.md §2.2 critical rule.
  </action>
  <verify>
    <automated>cd /Volumes/Tools/dev/chromium-src/src &amp;&amp; export PATH="$HOME/depot_tools:/opt/homebrew/bin:$PATH" &amp;&amp; caffeinate -is autoninja -j 8 -C out/Default unit_tests &amp;&amp; out/Default/unit_tests --gtest_filter='*LockedDarkHex*:*OnAccent_DarkIsBlack*' 2>&amp;1 | tail -5 | grep -q "PASSED"</automated>
  </verify>
  <done>Both Dark tests pass; never-white invariant has its own named test row.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh patches → produce 0112</name>
  <files>patches/0112-phlink-phase-08-03-default-dark-theme.patch</files>
  <action>
    Run `python3 scripts/refresh-patches.py --slot 0112 --subject "phlink: phase 08-03 default Dark theme (D-08 locked palette, kOnAccent=#000000)"` (or `git format-patch` fallback). Verify clean apply via `python3 scripts/apply-patches.py --dry-run`.
    Commit body must mention the never-white invariant for kOnAccent and reference D-08 §Dark table.
  </action>
  <verify>
    <automated>cd /Volumes/Tools/dev/phlink &amp;&amp; ls patches/0112-*.patch &amp;&amp; python3 scripts/apply-patches.py --dry-run 2>&amp;1 | tail -3 | grep -qi "ok\|clean\|success"</automated>
  </verify>
  <done>patches/0112-phlink-phase-08-03-default-dark-theme.patch exists and dry-run-applies cleanly.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Information Disclosure | unreadable Dark mode (white on yellow) | mitigate | Dedicated `OnAccent_DarkIsBlack_NeverWhite` gtest; failure blocks the build. |
| T-08-03-02 | Tampering | hex drift from stitch-output | mitigate | Per-token EXPECT_EQ assertions match D-08 table verbatim. |
</threat_model>

<verification>
- `out/Default/unit_tests --gtest_filter='*LockedDarkHex*:*OnAccent_DarkIsBlack*'` passes.
- `patches/0112-…patch` applies clean after 0111.
- WCAG-AA full pair-table audit: deferred to plan 08-05.
</verification>

<success_criteria>
1. kPhlinkDark[] carries the locked D-08 Dark hex.
2. kColorPhlinkOnAccent is unambiguously black for Dark mode (test names this invariant explicitly).
3. Patch 0112 lands cleanly on top of 0111.
</success_criteria>

<output>
After completion, create `.planning/phases/08-appearance-subsystem/08-03-SUMMARY.md`.
</output>
